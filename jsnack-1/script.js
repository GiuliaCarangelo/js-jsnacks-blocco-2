// Crea un array vuoto 
// e chiedi all'utente un numero da inserire nell'array. Continua a chiedere i numeri all'utente e a inserirli nell'array
// fino a quando la somma degli elementi è minore di 50.

let array = [];
let userInput = parseInt(prompt('inserisci un numero'));
array.push(userInput);
let arraySum = array.reduce;


while (somma < 50){
    userInput = parseInt(prompt('inserisci un numero'));
    array.push(userInput);
    if (arraySum < 50){

    }
}


